/*
* FINDING HTML ELEMENTS
* by id,classname,tags name,CSS selectors,HTML objects collections
*
*
* The node method
* DOM is a tree structure which has parent and children ,like u imagine
*
* node.childNodes - gives all the children under parent node
* node.firstChild - gives the first child of a node
* node.lastChild - gives the last child of a node
* node.hasChildNodes() - returns true if a node has children
* node.nextSibling - gives the next sibling of a node (difference are there between brother and cousin , )
* node.previousSibling - gives the previous sibling of a node
* node.nodeName - gives the name of a node
* node.nodeType - gives the type of a node
* node.nodeValue - gives the value of a node
* node.parentNode - gives the parent node of a node
* node.attributes - gives the attributes of a node
* node.ownerDocument - gives the document object of a node
*
*
*
*
*
* addEventListener() - attaches an event handler to the specified element,how shld event be handled,when bubbled
* */